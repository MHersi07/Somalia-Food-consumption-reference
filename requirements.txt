# Dependencies for the SIHBS 2022 consumption reference pipeline.
# Lower bounds reflect the minimum versions the code is written against;
# it was developed and verified with the versions noted in comments.
pandas>=2.0        # verified with 3.0.2
numpy>=1.24        # verified with 2.4.4
scipy>=1.10        # verified with 1.17.1
openpyxl>=3.1      # verified with 3.1.5  (Excel .xlsx reading)
